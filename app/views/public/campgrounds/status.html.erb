<div class="container">
  <div class="row mb-2">
    <h3>申請状況確認</h3>
   </div>
  <div class="row mb-2">
    <div class="col-md-10">
      <table class="table table-hover table-light table-borderless">
        <thead>
            <tr>
              <th>キャンプ場名</th>
              <th>申請者</th>
              <th>承認ステータス</th>
              <th>却下理由</th>
            </tr>
        </thead>
        <tbody>
            <% @campgrounds.each do |campground| %>
            <tr>
              <td><%= campground.name %></td>
              <td><%= campground.user.name %></td>
              <td><%= campground.is_confirmed_i18n %></td>
              <td><%= campground.rejection_reason %></td>
            </tr>
            <% end %>
            <%= paginate @campgrounds %>
        </tbody>
      </table>
    </div>
  </div>
  <div class=" text-center mb-2">
    <h5 class="mt-3">キャンプ場の情報をご存じでしたら情報提供にご協力をお願いします!</h5>
  </div>
  <div class="text-center mb-2">
      <%= link_to "情報を提供する", new_campground_path, class: "btn btn_orange rounded-pill w-50 m-auto my-2" %>
  </div>
</div>